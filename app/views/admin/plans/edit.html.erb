<% content_for :page_title, t(".title") %>

<div class="card">
  <div class="card-header">
    <h3 class="card-title"><%= t(".title") %></h3>
  </div>
  <div class="card-body">
    <%= render "form", form_url: admin_plan_path(@plan), form_method: :patch %>
  </div>
</div>
